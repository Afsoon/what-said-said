import Layout from '../../components/MDXLayout';
export default Layout;

export const description = 'Wrangler types generation are OS dependant version';
export const date = '2025-08-21';
export const updated_at = null;
export const tag = ["TIL"];

# Generate types with Wrangler

Today I just found that my worker script can't be in typescript as I can run `npx wrangler types` because my OS version it's two minor behind from the minimal.

It looks it's time to update if I want to test more Cloudflare Workers.
