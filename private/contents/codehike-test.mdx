---
title: "Codehike Test"
description: "Temporary post to verify codehike code blocks render with syntax highlighting"
date: "2026-08-14"
updated_at: null
tag: ["Testing"]
---

## TypeScript block

```ts
type Role = "admin" | "user" | (string & {});

interface User {
  name: string;
  role: Role;
}

export async function getUser(id: string): Promise<User> {
  const res = await fetch(`/api/users/${id}`);
  if (!res.ok) {
    throw new Error(`Failed to fetch user ${id}`);
  }
  return res.json();
}
```

## Bash block

```bash
pnpm add -D @rolldown/plugin-babel @babel/core
pnpm build && pnpm start
```

## Inline code

Inline code like `useMemo` should render with the existing `code` component, not codehike.
